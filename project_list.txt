Habit tracker (track daily habits, show streaks and completion percentage)
Text file search & replace (searches files for a string, shows matches, optionally replaces them)
Grade calculator (enters test scores, calculates average and letter grade)
CSV to JSON converter (read CSV, output formatted JSON)
Image metadata reader (shows EXIF data, dimensions, file size)
Watermark adder (adds text to images)
Email validator (checks if email addresses are formatted correctly)
CSV filter (reads CSV, filters by column value, outputs filtered CSV)
Text file merger (combines multiple text files into one)
Column summing tool (reads CSV, sums a specific column, shows breakdown)
Time zone converter (converts times between different timezones)
Book tracker (log books you've read, rate them, track reading progress)
Password strength checker (validates passwords against common criteria, gives feedback)
Data cleaner (removes duplicates, handles missing values in a CSV)
Statistics calculator (reads numbers from a file, calculates mean/median/mode/std dev)
Log file analyzer (reads a log file, counts error types, summarizes activity)
Regex tester (lets you test regex patterns against sample text)
Personal journal (write entries dated, search by keyword, display entries from a date range)
Expense tracker (log expenses with categories, show totals by category)
Backup script (copies files from one directory to another, tracks what was backed up)
Empty folder cleaner (finds and removes empty directories)
Bulk file renamer (rename multiple files at once with pattern matching)
Mortgage calculator (calculates monthly payment given principal/rate/years)
Unit converter (temperature, distance, weight, etc.)
File statistics tool (shows how many files, total size, breakdown by type in a directory)
QR code generator (takes a string/URL, generates a QR code image)
Dog facts fetcher (grabs random dog facts from an API)
Large file finder (searches a directory, finds and lists files over a certain size)
Loan calculator (calculates payoff schedule with interest)
Duplicate file finder (searches a directory, finds identical files by hash, shows which ones could be deleted)
Simple to-do list (add/remove/mark tasks, save to file, load on startup)
Screenshot organizer (sorts screenshots by date into folders)
Directory tree visualizer (prints out a nice tree structure of folders and files)
Currency converter (uses an API to convert between currencies)
File age analyzer (shows which files haven't been modified in X days)
Text statistics analyzer (word count, character count, most common words, reading time estimate)
BMI calculator (takes height/weight, calculates BMI, gives health category)
Symlink manager (creates/removes symbolic links, lists existing ones)
Birthday reminder (stores birthdays in a file, tells you who has one coming up)
Data validator (checks if CSV data meets certain criteria, reports errors)Word frequency counter (reads a text file, counts word occurrences, shows top N)
JSON formatter/validator (takes messy JSON, prettifies it, catches errors)
BMI and calorie counter (tracks daily calorie intake, calculates totals)
Countdown timer (sets a timer for X minutes, counts down, alerts when done)
